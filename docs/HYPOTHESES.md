# HYPOTHESES

H1–H9 are transcribed from PDF §6. The **Experiment**, **Metric** and **Falsification** columns turn the PDF's criteria into operational tests. Where our wording is stricter or more specific than the PDF's, that is marked *(MOG)*.

Status values: NOT TESTED / IN PROGRESS / SUPPORTED / FALSIFIED / INCONCLUSIVE. A status changes only through an entry in `EXPERIMENT_LOG.md` backed by results in `results/raw/`.

The "earliest stage" column gives the first rung of the ladder (RESEARCH_PLAN.md §4) where a *meaningful* test is possible. Runs at tiny scale only debug the pipeline for these tests.

| # | Hypothesis (PDF) | Prediction (PDF) | Experiment | Metric | Falsification criterion | Earliest stage | Status |
|---|---|---|---|---|---|---|---|
| H1 | Optimal geometry is genuinely heterogeneous across blocks. | An oracle assignment (per-block short-rollout search at frozen checkpoints) beats the best tuned homogeneous optimizer at matched tokens. | Exp 1 (atlas: does heterogeneity show up in the measurements?) and Exp 2 (oracle rollouts). Then a full run with the oracle assignment vs tuned AdamW / Muon / hybrid. | Δ val loss (oracle − best homogeneous) at matched tokens, ≥3 seeds. *(MOG)* Also the fraction of blocks whose oracle argmax differs from the folklore assignment. | Oracle gain lies within seed noise (PDF). *(MOG)* Concretely: the 95% CI of the paired difference includes 0 at ≥3 seeds. | C (~1M) | NOT TESTED |
| H2 | The free proxy $E_c=\theta_c/\hat L_c$ identifies the oracle's choice. | Rank correlation between $E_c$ and rollout loss reduction >0.7. The proxy recovers ≥70% of the oracle gain. The optimal k shrinks as gradient $r_{\rm eff}$ collapses over training. | Exp 2: at frozen checkpoints, compare the per-block ranking of candidates by $E_c$ with their ranking by realized h-step loss reduction. Exp 1 tracks $r_{\rm eff}$ over training. | Spearman ρ(E_c, ΔL) per block (median and distribution), top-1 agreement, and fraction of oracle gain recovered. Trend of $k^\star$ vs $r_{\rm eff}$. | Proxy uncorrelated with rollout outcome. $k^\star$ constant (PDF). *(MOG)* median ρ ≤ 0.3 or top-1 agreement no better than chance. | C | NOT TESTED |
| H3 | Granularity–batch law (Prop. 4). | $k_{\min}\propto1/B_{\rm tok}$ over a 16× batch sweep. Fine granularity helps only above the predicted batch threshold. | Batch sweep (≥16×) with a fixed geometry family and varying k. Measure $\hat\lambda$ per k. | Fitted slope of $\log k^\star$ vs $\log B_{\rm tok}$. | $k^\star$ flat across the sweep (PDF). | D (~5M) | NOT TESTED |
| H4 | Symmetry-matched grouping wins (§3.2). | Per-head grouping on Q/K/V/O, per-expert grouping in MoE and per-token rows on the embedding and head each beat whole-matrix Muon **and** beat a random partition with the same k. | Matched-budget runs: symmetry-matched vs random partition at equal k vs full Muon. | Val loss at matched tokens, ≥3 seeds. | Symmetry-matched ≈ random partition at equal k (PDF). | C/D | NOT TESTED |
| H5 | Duality's per-token prescription for embeddings/heads (§2.2). | Per-token ℓ2-normalized updates beat AdamW on the embedding + unembedding at matched budget, and the gain is concentrated on rare tokens. | Swap only the embedding and head optimizer (AdamW → RowNorm rows) with everything else fixed. | Val loss. Per-token loss stratified by frequency decile. | No gain, or a gain that is not frequency-stratified (PDF). | C (vocab must be realistic, so needs a real tokenizer) | NOT TESTED |
| H6 | Alignment sets the width exponent (Prop. 5). | Measured $\chi_\ell$ predicts each layer's empirical LR-transfer exponent across a 4× width sweep. | Width sweep (≥4×) with a per-layer LR sweep. Measure $\chi_\ell$ from activations and updates. | Correlation between predicted and fitted per-layer exponents. | Exponents uniform, or unrelated to $\chi_\ell$ (PDF). | D/E | NOT TESTED |
| H7 | Coupling sets the LR ceiling (Prop. 3). | Measured $\lambda_{\max}(\tilde M)$ predicts the max stable global LR across geometry assignments and architectures. | Estimate $\lambda_{\max}(\tilde M)$ by power iteration. Run an LR sweep to find the divergence threshold. | Correlation between $1/\lambda_{\max}$ and the empirical max stable LR. | No correlation between $\lambda_{\max}$ and the empirical ceiling (PDF). | C/D | NOT TESTED |
| H8 | Gains are depth-localized. | Applying granular geometry only to the first and last blocks captures most of the benefit. (The PDF cites arXiv:2606.04662, whose relevant statement is appendix-level and about within-layer sharpness; see THEORY.md D4.) | Compare granular-everywhere vs boundary-only vs folklore. | Fraction of the granular-everywhere gain captured by boundary-only. | Gains distributed uniformly through depth (PDF). | D | NOT TESTED |
| H9 | *Negative:* diminishing returns below the neuron. | Per-entry geometry selection adds nothing over per-neuron (predicted by Adam-mini's per-block sufficiency). | Per-entry vs per-neuron selection at matched budget. | Val loss difference. | Per-entry selection gives a further clear gain (PDF). | D | NOT TESTED |

Notes:
- The PDF calls H5 and H8 "the cheapest high-value experiments" at 124M. At our scale they are also cheap single-tensor or two-block interventions. H5 needs a realistic vocabulary and token-frequency distribution, or the rare-token stratification means nothing.
- Every H is currently NOT TESTED. The synthetic Exp 0 tests none of H1–H9. It only checks that the PDF's closed-form and synthetic claims are computed correctly.
