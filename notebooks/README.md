# notebooks/

Exploration and visualization only. Canonical results always come from `results/raw/*/results.json` and `metrics.csv`, which are produced by scripts, and never from a notebook.

Planned notebooks, created when they are first used:

| Notebook | Contents |
|---|---|
| `01_synthetic_geometry.ipynb` | Browse the Exp 0 outputs |
| `02_geometry_atlas.ipynb` | Explore `atlas.csv` |
| `03_oracle_vs_proxy.ipynb` | Explore `oracle.csv` |

The interactive results page, `reports/atlas_report.html`, is built with `python -m scripts.generate_plots`.
